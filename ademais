
function numerosPiramidais(n,r)
    numero = (n^2/2) + n ^ 3 *((r/6)-(1/3)) - n * ((r-5)/6)
    return numero
end

function numeroFermat(n)
  numero = (2^(2^n))+1
  return numero
end

function indicek(fa,fc)
   return fa/fc
end
